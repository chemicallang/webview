public namespace webview {

public enum WindowHint : int {
    // Width and height are default size
    None = 0
    // Width and height are minimum bonds
    Min = 1
    // Width and height are maximum bonds
    Max = 2
    // Window size can not be changed by a user
    Fixed = 3
}

}
